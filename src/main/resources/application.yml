# Datasource
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/db_miniapps_employee
        username: postgres
        password: pgsqlpassword1
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
